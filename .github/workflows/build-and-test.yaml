---
name: "Build and test"
on: ["push"]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Cache Cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Check production mode compiles
        run: cargo check -F production

      - name: Get entropy binary from latest release
        run: |
          docker pull entropyxyz/entropy:test-release-outtie-test-01
          docker create --name extract-entropy-binary entropyxyz/entropy:test-release-outtie-test-01
          docker cp extract-entropy-binary:/usr/local/bin/entropy ./entropy
          docker container remove extract-entropy-binary

      - name: Run tests
        run: ENTROPY_NODE=$(pwd)/entropy cargo test --release
